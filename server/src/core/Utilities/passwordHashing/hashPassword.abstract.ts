// import { injectable } from 'inversify';

// @injectable()
// export abstract class HashAndComparePassword {
//   protected static saltRounds = process.env.SALT_ROUNDS || 8;

//   abstract hashPassword(plainText: string): string;
//   abstract comparePassword(plainText: string, encryptedValue: string): boolean;
// }
